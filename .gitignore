# deps / build
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# env — NUNCA commitear: el repo es PÚBLICO
.env
.env.local
.env.*.local

# misc
.DS_Store
npm-debug.log*

# diseño fuente (bundle pesado) — mantener fuera del repo público
design/
